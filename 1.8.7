# 野生拟合认知模型 v1.8.7 升级说明
## 版本定位
v1.8.7 是对模型**信号输入格式**和**时间维度**的底层修正。前序 v1.8.6 隐含两项理想化简化假设：
1. 信号为单点事件，采用单一功耗值 + 单一时间戳描述；
2. 连续冲击相互独立，不存在能量叠加效应。

真实物理系统不满足上述前提。v1.8.7 移除两项假设，**不改动模型全部核心操作链与基础公理**，仅重构信号描述规范、实例集存储结构。

> 核心约束：同一层级内，信号包络所有分量共享该层统一场限参数；各分量差异仅体现在功耗、穿透深度、衰减曲线、恢复窗口，过载判定、稳态裕度、适配演化规则共用一套场限。远期若需要分量独立稳态约束，优先采用纵向分层方案，不在单层内嵌入多套动力学规则，避免破坏「层=单一动力学场域」基础定义。

## 一、信号包络：复合信号的多层并行穿透路径
### 变更前（v1.8.6）
信号等价于抵达边界的物理扰动，以单点功耗标量描述单次冲击。

### 变更后（v1.8.7）
信号定义为**信号包络**，由多维度并行特征构成。同一物理冲击抵达层边界时，不同信号成分阻抗、穿透深度、衰减速率存在差异，沿独立路径并行穿透，生成一组并行子记录。

信号包络基础维度（可按需扩展）：
- 功耗包络：幅值、上升沿、下降沿；
- 频率包络：频谱分布特征；
- 时序包络：节奏、间隔模式；
- 关联包络：上下文锚点、跨模态绑定线索；
- 情绪包络：语调、情绪调制分量。

#### 关键规则
1. **同源绑定**：所有子记录共享统一**冲击标识（事件ID/相位标记）**。后续跨簇交汇、统计分布依靠该标识区分「同源多成分耦合」与「无关信号偶然重合」；
2. **时间基准**：全部子记录共用冲击绝对时间戳；每条子记录独立存储功耗、穿透深度、衰减曲线、恢复时间 $T_r$；
3. **缺省规范**：任一维度无有效输入时，写入固定缺省数值（如0），禁止空值。系统可将「维度长期缺失」识别为稳定模式；例如长期无情绪分量形成中性信号密集区，情绪分量突现可判定为异常扰动。

#### 实例集变更
原实例集存储「单次穿透的单点功耗特征」；
新版本实例集存储**单次冲击对应的子记录集合**。层级统计时，既可对单一维度独立对齐，也可执行跨维度、跨子记录的跨簇交汇运算。

#### 场景映射规范
- 文本信号包络：语义内容 + 情绪轮廓 + 节奏模式 + 上下文锚点；
- 音频信号包络：波形幅值 + 音色特征 + 语调曲线 + 背景噪声分布；
- 视觉信号包络：图像内容 + 空间频率 + 色彩梯度 + 时序变化。

## 二、震荡持续期：冲击的时间残余效应
### 变更前（v1.8.6）
冲击写入实例集后，系统瞬时回归基线；默认连续冲击相互独立运算。

### 变更后（v1.8.7）
冲击注入能量后，层场限进入**阻尼震荡持续期**，震荡幅值随时间逐步衰减，直至回归基线。
定义参数 $T_r$：**无外部新冲击条件下，成分衰减至基线的理论恢复时间窗口**。

$T_r$ 动态影响因子：
- 冲击功耗幅值越高，$T_r$ 越长；
- 层级场限参数 $C$、带宽 $\sigma$ 越大，$T_r$ 越长；
- 适配层阻尼系数越高，能量耗散越快，$T_r$ 越短。

#### 时序叠加规则
在 $T_r$ 窗口内，层级处于残余震荡态；新冲击抵达时，信号叠加在残余状态之上，不再独立运算。
每条冲击记录附带元数据：冲击到达时层级状态（基线 / 轻度残余 / 重度残余）。
> 备注：基线、轻度残余、重度残余为可读标签，底层绑定残余能量占场限裕度的量化阈值，保障仿真可复现。

场景示例：高情绪幅值文本拥有更长 $T_r$；后续输入信号的匹配统计，必须携带前序残余状态作为条件，区分稳态长期关联与震荡临时耦合。

## 三、两套机制自然衔接
同一冲击包络内，不同信号成分衰减特性不一致，一条冲击记录存储一组 $T_r$ 分布，而非单一恢复时长。
典型表现：语义成分恢复快（数百毫秒），情绪成分恢复缓慢（数秒至数分钟）。
新冲击到达时，系统按**成分粒度**判定残余状态，而非简单判定层级整体是否震荡。
收益：跨簇交汇可清晰区分两类关联：
1. 基线稳态下形成的长期稳定关联；
2. 残余震荡叠加生成的临时耦合。

### 实例记录结构样板
冲击事件
├─ 冲击标识：Event-ID
├─ 绝对时间戳：T0
├─ 冲击抵达时层级残余状态：基线 / 轻度残余 / 重度残余
└─ 多成分子记录集合
├─ 语义成分：功耗 X，穿透深度 Y，Tr=0.8s
├─ 情绪成分：功耗 X'，穿透深度 Y'，Tr=4.2s
└─ 上下文成分：功耗 X''，穿透深度 Y''，Tr=1.5s


> 补充规则：同冲击内各分量并行穿透，分量之间不发生实时互调制；分量交互仅体现在后续统计与跨簇交汇阶段。

## 四、与 v1.8.6 退化机制衔接
v1.8.6 已定义结构记录匹配、实例渐进退化机制。v1.8.7 提供精细化实现路径：
各成分 $T_r$ 不一致，退化速率存在天然差异。单次同源冲击的实例记录会出现**局部匹配、局部退化**现象：部分子记录持续匹配场限长期留存，另一部分快速衰减退化为噪声。
这是结构性退化的细化表达：实例不会整体失效消失，而是各信号成分逐步脱离有效匹配区间。

## 五、外部接入使用规范
接入 v1.8.7 的载体（仿真程序、Agent）必须将原始输入显式展开为标准化信号包络，禁止直接送入单点标量信号。
1. 特征维度不必全部具备，但字段位置固定预留；
2. 缺失维度填充固定缺省值参与统计；
3. 信号来源无法溯源时，对象标识字段允许置空。

## 六、升级边界说明
1. **完全保留 v1.8.6 所有核心定义**：场限、四类响应、适配层、层间交互、完整操作链逻辑不变；
2. 唯一变更：实例集内部存储结构，由单条记录升级为支持多子记录集合；
3. 操作链步骤顺序、触发条件维持原样，仅「写入实例集」环节输出结构调整。

## 七、版本向前兼容策略
v1.8.6 单点格式记录在 v1.8.7 解释器中，自动识别为**仅含单一成分的信号包络特例**。
新旧记录可共存于同一实例集，无需批量数据迁移、离线格式转换。

## 八、版本关键差异对照表
|项目|v1.8.6|v1.8.7|
| ---- | ---- | ---- |
|信号形态|单点功耗标量|多维度信号包络，并行多子记录|
|同源关联|无冲击标识|子记录共享冲击标识，区分同源信号|
|空维度处理|空值，不参与统计|固定缺省值，可识别“信号缺失”模式|
|冲击时序关系|冲击相互独立|支持残余震荡叠加，区分基线/残余态|
|恢复假设|冲击后瞬时回归基线|引入阻尼震荡持续期，各成分独立$T_r$|
|时序轨迹|独立扰动序列统计|残余叠加条件下的复合时序记录|
|实例记录单元|单次单点记录|同源冲击下多子记录集合|

### 操作链文字对比
**v1.8.6 链路**
物理扰动 → 适配层慢变调制 → 冲击层边界节点 → 超阈值导通产生功耗 → 写入实例集，附带时序轨迹

**v1.8.7 链路**
物理扰动（信号包络）→ 适配层慢变调制 → 冲击层边界节点 → 各成分按阻抗并行穿透，生成共享冲击标识与时间戳的子记录集合 → 写入实例集；每条子记录携带功耗、穿透深度、衰减曲线；整条冲击附加到达时刻残余状态元数据。

## 九、文档修订点位
1. 《层的固有属性与响应》- 实例集章节：新增子记录、冲击标识、缺省值处理规则；
2. 《信号与含义的物理起源》：使用「信号包络」定义替换原有单点信号定义；
3. 《完整操作链》：重写首段信号输入流程，补充残余状态判定逻辑。

## 十、核心思想总结
本次迭代移除旧版本两大理想化假设：信号单点化、冲击时序相互独立。
通过信号包络实现多分量并行穿透建模；通过阻尼震荡残余项，还原真实系统能量不会瞬间消散、新旧信号持续叠加的动力学特征。
本次迭代属于**输入侧数据表示升级，模型底层公理体系无改动**，兼容性强、迭代风险可控，显著提升模型对连续时序事件、多模态混合刺激、情绪滞后、记忆差异化消退现象的拟合能力。
