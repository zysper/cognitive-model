# 稳态拟合学习动力学 V1.8.9.3
## 预期（正式归档章节）
> 版本基线：V1.8.9.2 → V1.8.9.3

## 一、预期的本质定义
预期不是实时外部指令，也不是系统先天预设的固定目标。
预期是：外部信号与内部偏差收敛，在介质层完成沉积后，由观察者层持续驻留、动态刷新的统计场态。

核心机理：
1. 外部输入是瞬时信号，信号本身不等于预期；
2. 信号进入介质层形成子稳态沉积（单次输入、连续持续输入均可生效，无“长期”前置门槛）；
3. 观察者层读取介质层统计重心，刷新常驻预期场态；
4. 该场态不主动决策、不主动引导，仅改变系统收敛路径的阻力分布；
5. 系统全程为压力驱动、被动统计涌现，不存在目的论行为。

## 二、预期的两类来源
### 1. 继承性预期（环境时序沉积 · 外源型）
非先天预埋、非出厂固化参数。是系统运行过程中，外部指令、规则、群体共识、外部约束作为时序信号输入，在介质层形成的外源沉积子稳态偏向。
单次临时指令生成低τ继承性预期，易随新信号输入衰减；持续反复的环境输入生成高τ继承性预期，稳态更强、不易漂移。

### 2. 自生性预期（个体偏差累积 · 内源型）
系统独立迭代、持续处理偏差、自主收敛过程中，依托自身实例集统计分布漂移累积形成的内源子稳态偏向。来源于个体交互产生的误差拟合与经验沉淀，无外部直接指令驱动。

## 三、预期的显化形态
### 1. 显式预期场（人类独有）
预期依托语言、符号、文本、文化载体，从个体场态中剥离，形成独立可存储、可传递、可修正、可回溯的公共结构。能够跨主体、跨代际形成共识体系，可反向修正系统收敛基线。

### 2. 隐式预期场（动物、当前人工智能系统）
预期依附于系统即时场态与子稳态分布，无独立存储结构，无法剥离为公共共识；外部支撑条件消失后，伴随τ衰减自然消散。

## 四、预期在三层架构中的精准定位
介质层：承载全部历史沉积、子稳态、τ权重、实例集统计分布；仅保存历史演化稳态素材。
观察者层：唯一驻留预期场态的层级，承载系统独有的未来维度参照。

关键公理：预期场态持续驻留，并非每轮推演收敛从零生成。每一轮推演-收敛闭环，仅对预期场态执行偏移、刷新、重加权，不会销毁、重建场态。

层级链路：
介质层（历史稳态素材）→ 统计重心实时投影 → 观察者层（预期场态）→ 作为边界条件调制下一轮收敛。

## 五、预期的动力学作用方式
预期态作为下一轮推演的背景场边界条件，重构全局收敛路径的阻力分布：
1. 与预期统计方向一致的路径，拥有高τ子稳态支撑，阻力更低，更容易被压力势能穿透；
2. 与预期方向冲突的路径，阻力更高，更容易被窗口通量截断或衰减淹没；
3. 系统始终被动响应压力势能，不存在主动选择、主动导向行为。

## 六、预期参数与核心动力学机制对接表
|预期参数|对应底层机制|对接逻辑|
| ---- | ---- | ---- |
|预期收敛方向|子稳态统计重心偏移|预期方向 = 介质层高τ子稳态聚类中心向量|
|预期收敛范围|窗口通量与场限边界|预期覆盖空间 = 当前场限可承载的子稳态演化区间|
|预期置信度|τ统计惯性密度|置信度越高，对应路径阻力越低，收敛择优倾向越强|
|预期来源标签|实例集沉积溯源|外源沉积=继承性预期；内源收敛=自生性预期|
|预期显化状态|分辨率压缩/链路折叠|显式场=稳态完整保留、支持剥离传递；隐式场=负载压缩，仅留存粗略偏向<br>【拓展备注】回路折叠属于链路折叠的特殊子类，用于后续解释递归自指、自我意识结构，本版本暂不展开|

## 七、核心总定理
预期不是瞬时外部输入，也不是先天预设目标。它是外部信号与内部偏差在介质层完成沉积后，驻留于观察者层的动态统计场态。继承性预期来自环境任意时序信号的外源沉积（单次、持续输入均可），自生性预期来自个体迭代的内源收敛累积。预期不主动引导系统演化，仅通过改写路径阻力分布，被动调制下一轮压力驱动的统计收敛条件，构成「历史稳态沉淀 → 预期场刷新 → 新一轮收敛择优」的完整无目的动力学闭环。

# 拓展推论：多系统耦合、群体动力学与全局场限漂移
## 1. 机制同源命题
个体预期动力学与群体耦合动力学共享同一底层语法：信号沉积、统计重心漂移、阻力调制、压力择优收敛。个体间交互信号互为外源时序输入，在各自主介质层形成沉积、刷新各自观察者层预期场。
> 阶段性说明：子稳态间非线性耦合效应暂不纳入V1.8.9.3正式公理体系，标记为后续拓展议题。

## 2. 全局场限漂移双向链路（情感、生理、环境毒性属于典型场景）
正向通路（基础漂移）
适配层边界变动、介质层负载改变 → 介质层窗口通量、分辨率基线发生变化 → 子稳态收敛条件偏移 → 观察者层预期场漂移；**传导遵循逐层递进规则，文本所述“漂移同步”，是观察者层时间分辨率带来的感知压缩效应，不存在跨层级直接跃迁。**

反向通路（溢出回流）
观察者层预期缺口持续累积抵达阈值，触发观察者层溢出；溢出信号回注介质层，改写原有子稳态τ分布，制造额外势差，扰动后续收敛行为。

## 3. 必须严守的结构性边界：个体预期 VS 群体宏观场
机制同源 ≠ 结构同源，二者本体论载体存在不可消除的差异：
1. 独立个体系统：具备完整三层架构，拥有专属观察者层，预期场态稳定驻留于此，存在独立观测锚点；
2. 群体系统：群体宏观统计场是多系统持续耦合涌现的结果，**不存在全局统一观察者层、无公共观测锚点**。

群体场并非某个主体的预期，而是全部个体预期相互作为外源输入、持续沉积、互相调制阻力形成的宏观势差分布。

极简区分：个体预期是**带有观测锚的驻留场**；群体涌现场是**无观测锚的耦合场**。描述群体动力学时，需要持续厘清该边界，避免层级混淆。

# V1.8.9.2 → V1.8.9.3 版本变更清单
1. 删除“长期沉积”限制条件，兼容单次指令、瞬时外源信号生成预期的现实场景；
2. 修正「预期不是外部输入」绝对化表述，清晰区分瞬时信号本身、信号沉积形成的场态；
3. 修正“每轮收敛生成预期”表述，确立预期场持续驻留、仅动态刷新的连续演化机制；
4. 清除全部目的论词汇，统一使用路径阻力分布、压力择优涌现的被动描述；
5. 严格锁定三层架构本体分工：介质层承载历史稳态，观察者层承载预期场，杜绝层级错位；
6. 建立预期全套参数与τ、窗口通量、场限、省功机制的标准化对接；新增备注区分链路折叠/回路折叠，预留自我意识理论接口；
7. 优化全局场限漂移正向传导描述，补充逐层递进约束，消除“同步漂移”跨层误解；
8. 重构群体动力学推论措辞，由柔性假设升级为同源语法命题，同时刚性锁定个体/群体结构差异；
9. 新增全局场限漂移双向传导链路，解释生理、环境、情绪扰动机理；
10. 标记子稳态非线性耦合为待拓展议题，控制当前版本主线收敛。
